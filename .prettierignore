# Build output
**/dist/
playground/dist/
site/dist/
# Deps + lockfiles
**/node_modules/
bun.lockb
*.lock
# Generated / snapshots / coverage
scripts/gap-matrix.md
**/__snapshots__/
**/*.snap
**/coverage/
**/.astro/
**/.vite/
# Agent worktrees + scratch
.claude/worktrees/
# Data / binary-ish fixtures
**/*.xgis
**/*.geojson
**/*.pbf
**/*.png

# Vendored external standard — the IHO S-111 catalogues are kept byte-for-byte as the
# single-authority reference (docs/standards/s-111/README.md is ours and stays formatted).
docs/standards/s-111/portrayal/
docs/standards/s-111/111_Feature_Catalogue_2.0.0.xml

# Generator-owned coverage report — the emitter is the single format authority
# (a prettier reflow here just gets rewritten by the next full vitest run).
compiler/src/__tests__/__coverage__/SUMMARY.md

# Vendored upstream snapshot — the MapLibre demotiles mirror is kept byte-for-byte so the
# committed style is the same bytes the live demo fetches (the #1495 repro fixture; the
# premise is stated at playground/src/demos/fixtures.ts:409). Its .pbf glyph ranges are
# already covered by **/*.pbf above; these two .json files were not.
playground/public/vendor/

# Generator-owned API reference — `bun run build` emits it (site/.gitignore excludes it from
# the repo entirely), so prettier only ever meets it on a machine that has built. Formatting
# it is waste that the next build overwrites, and leaving it in made `format:check` report
# 395 red files after any local build — the noise this file exists to prevent.
site/src/content/api/
