{
  "_doc": "Twin-parity pixel ratchet (#1046 F3-F4, doc §6.3). Per-fixture HIGH-WATER DC (differing-pixel count) of the unified WebGL2 chain (?rhichain=1) vs the forced-WebGL2 twin at IDENTICAL cameras. SHRINK-ONLY per fixture: a value may only decrease; zero on EVERY fixture is F4's flip precondition; deleted with the twin in F6. null = NOT YET MEASURED — the orchestrator seeds the real value from the first ?rhichain=1 capture run (playground/e2e/_twin-parity-ratchet.spec.ts), which OWNS the live DC<=high-water assertion; the vitest gate (twin-parity-ratchet.test.ts) validates this file's shape + vacuity and SKIPS null entries. The fixtures set is the doc §3-F3 matrix and must stay in lockstep with the gate's CANONICAL_FIXTURES. ZEROED 2026-08-04 (from the 2026-08-04 500-cushion reseed after the Inc-E2 terminal port, which had collapsed the content-less-chain seeds 188000/181000/45000/131500 to 71/126/70/102 = HUD-clock digits only): two harness determinism fixes took the four measurable rows to EXACT DC=0, twice consecutively — (1) the demo error-HUD clock is FROZEN to '--:--:--' under ?e2e=1 (demo-runner.ts fmtRow; the digits were the last residue), and (2) every fixture's camera hash is PINNED in the spec, because the demo's post-compile bounds-fit fires PER SOURCE COMPILE and a loaded-runner capture can land BETWEEN fits (measured live: a dashed_lines twin arm at the land-only fit lat=-3.17744 vs its chain arm at the final land+coast fit lat=-0.98195 -> DC 34182 of pure camera offset — a protocol flake, NOT a render regression; the pinned re-runs measured 0). Ladder status: fixture_line_image_pattern + fixture_translucent_outline are BYTE-IDENTICAL PNGs across arms (rung-3 hash equality); minimal + dashed_lines are DC=0 at the |delta|>8 channel threshold with sub-threshold AA noise remaining (rung 2). DC=0 here is F4's precondition MET for these four rows. ofm_* stayed null: UNMEASURABLE on this runner — the PAIR exceeds the 300s test budget end-to-end, and the timeout hits at TEST level, outside the twin-arm skip guard (proven by the run's 'Test timeout of 300000ms exceeded' failure text). ARM ATTRIBUTION IS UNPROVEN (review F1): a wedged twin evaluate() (no default timeout) and two slow-but-under-cap arms both reach the same test-level timeout, and no per-arm artifact survives. The spec now prints per-arm start markers so the next ofm-including run attributes the budget; the entries wait for that run or a faster runner.",
  "fixtures": {
    "minimal": 0,
    "ofm_bright_local_flat": null,
    "ofm_bright_local_globe": null,
    "dashed_lines": 0,
    "fixture_line_image_pattern": 0,
    "fixture_translucent_outline": 0
  }
}
