{
  "_doc": "Twin-parity pixel ratchet (#1046 F3-F4, doc §6.3). Per-fixture HIGH-WATER DC (differing-pixel count) of the unified WebGL2 chain (?rhichain=1) vs the forced-WebGL2 twin at IDENTICAL cameras. SHRINK-ONLY per fixture: a value may only decrease; zero on EVERY fixture is F4's flip precondition; deleted with the twin in F6. null = NOT YET MEASURED — the orchestrator seeds the real value from the first ?rhichain=1 capture run (playground/e2e/_twin-parity-ratchet.spec.ts), which OWNS the live DC<=high-water assertion; the vitest gate (twin-parity-ratchet.test.ts) validates this file's shape + vacuity and SKIPS null entries. The fixtures set is the doc §3-F3 matrix and must stay in lockstep with the gate's CANONICAL_FIXTURES. RESEEDED 2026-08-04 after the Inc-E2 terminal port (VTR immediate arm + renderToPass immediate skip): measured DC collapsed from the content-less-chain seeds (188000/181000/45000/131500) to 71/126/70/102, and the S5 16-split read showed 15/16 tiles EXACTLY zero on every fixture — the 16th (bottom-right) differs only in the demo harness error-HUD wall-clock digits ('[h:mm:ss AM]' glyphs; the panel itself appears in BOTH arms). Map pixels are identical. Seeds = 500, a clock-digit-variance cushion above the ~30 px same-code SwiftShader jitter measured earlier; the path to literal zero (F4's precondition) is suppressing the HUD timestamp under e2e=1, not tolerance. ofm_* stayed null: UNMEASURABLE on this runner — the pair blew a 300s per-test budget end-to-end (arm undetermined; the twin arm alone needed >35s pre-flip, and the chain arm cannot converge while the VT terminals are native-bodied), so the entries wait for either the terminal port or a faster runner.",
  "fixtures": {
    "minimal": 500,
    "ofm_bright_local_flat": null,
    "ofm_bright_local_globe": null,
    "dashed_lines": 500,
    "fixture_line_image_pattern": 500,
    "fixture_translucent_outline": 500
  }
}
