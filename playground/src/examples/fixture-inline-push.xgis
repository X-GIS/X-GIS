/* Fixture: inline source (no url). The host app must call
 * setSourceData() after run() to populate the feature collection.
 * Used by the external data-injection e2e test. */
source tracks { type: geojson }
layer dots {
  source: tracks
  | fill-rose-500 size-40
}
