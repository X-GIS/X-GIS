xgis 1

// NOAA S-111 surface currents (#1272) — a SYNTHETIC Chesapeake-Bay-shaped
// tidal field (northward channel + counter-clockwise gyre, coast-shaped nodata
// margins) carrying the real S-111 band names/units/fill. The speed band draws
// through the viridis coverage colour ramp (range 0-2 kn); the demo runner
// overlays drifting particles along the direction band (Demo.currents →
// retained particle-flow, #826). The `coverage` source reads the S-111 HDF5 IN
// PLACE (ADR-0010, no `.xgcov` transcode); regenerate the asset with
// `python playground/scripts/gen-demo-coverage.py`.

// Esri World Imagery satellite basemap (same source the satellite_map demo uses) —
// the currents draw OVER real imagery for real-world nautical context. CORS `*`, so it
// works on the hosted static site. {z}/{y}/{x} = the ArcGIS path order.
source basemap {
  type: raster
  url: "https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}"
}

source currents {
  type: coverage
  url: "synthetic-currents.h5"
}

layer imagery {
  source: basemap
}

layer speed {
  source: currents
  ramp: "viridis"
  range: [0, 2]
  | opacity-70
}
