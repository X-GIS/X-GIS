/* Fixture: inline source populated via the typed-array fast path
 * (setSourcePoints). Identical .xgis to fixture-inline-push; the
 * difference is which API the e2e test calls to inject data. */
source tracks { type: geojson }
layer dots {
  source: tracks
  | fill-rose-500 size-40
}
